# Live evidence fixtures

Artifacts served from this commit and referenced by
`tests/integration/test_end_to_end.py` on `main`. The contract fetches
and verifies them during live adjudication. Do not rewrite this branch:
the evidence commitments pin its commit.
